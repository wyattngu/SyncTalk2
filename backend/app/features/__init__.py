# backend/app/features/__init__.py
#
# Feature-folder layout. Each subpackage groups the model + repository +
# service + controller for one bounded domain. New backend features should
# live here rather than scattered across the legacy flat directories
# (controllers/, services/, repositories/, models/).
